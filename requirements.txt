# Core -- Claude Agent SDK
# No upper bound to avoid blocking patch releases, but pinned to a known-good minor.
claude-agent-sdk>=0.1.0

# Environment variable loading
python-dotenv>=1.0.0

# HTTP client (used by SDK)
httpx>=0.27.0
anyio>=4.4.0

# Pydantic (used by SDK)
pydantic>=2.7.0
pydantic-settings>=2.3.0

# Note: No arcadepy -- GitHub uses gh CLI directly
